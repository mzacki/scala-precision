package edu.ant
package basic

object Dojo extends App {

  //def squareSum(xs: List[Int]): Int =
    //xs.map(a => a * a)

    val list = Array(true, false, null)

    println(list.count(a => a == true))
}
